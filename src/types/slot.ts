export interface Slot {
    slotId: string;
    name: string;
    startTime: string;
    endTime: string;
    capacity: number;
    sessionDuration: number;
    doctorId: string;
    updatedAt: string;
}
